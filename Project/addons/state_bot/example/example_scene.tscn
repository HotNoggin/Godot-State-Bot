[gd_scene load_steps=5 format=3 uid="uid://dc78g254s4flf"]

[ext_resource type="Script" path="res://addons/state_bot/state_bot.gd" id="1_wbel7"]

[sub_resource type="GDScript" id="GDScript_yp2gj"]
script/source = "extends SimpleState
## This is a brand new SimpleState! You can use the functions below to create some new behavior.
## If you would like to reuse this state elsewhere, use the \"Save As\" option in the script \"File\" tab.
## If you meant to use an existing state, simply set this node's script to that state instead.


## Called once automatically when this state is entered.
func _enter_state(last_state: SimpleState) -> void:
	print(\"State entered\")


## Called once automatically when this state is exited.
func _exit_state(new_state: SimpleState) -> void:
	pass


## Called every frame while this state is active.
func _state_process(delta: float) -> void:
	pass


## Called every physics tick while this state is active.
func _state_physics_process(delta: float) -> void:
	pass
"

[sub_resource type="GDScript" id="GDScript_qy167"]
script/source = "extends SimpleState
## This is a brand new SimpleState! You can use the functions below to create some new behavior.
## If you would like to reuse this state elsewhere, use the \"Save As\" option in the script \"File\" tab.
## If you meant to use an existing state, simply set this node's script to that state instead.


## Called once automatically when this state is entered.
func _enter_state(last_state: SimpleState) -> void:
	pass


## Called once automatically when this state is exited.
func _exit_state(new_state: SimpleState) -> void:
	pass


## Called every frame while this state is active.
func _state_process(delta: float) -> void:
	pass


## Called every physics tick while this state is active.
func _state_physics_process(delta: float) -> void:
	pass
"

[sub_resource type="GDScript" id="GDScript_2w0hy"]
script/source = "extends SimpleState
## This is a brand new SimpleState! You can use the functions below to create some new behavior.
## If you would like to reuse this state elsewhere, use the \"Save As\" option in the script \"File\" tab.
## If you meant to use an existing state, simply set this node's script to that state instead.


## Called once automatically when this state is entered.
func _enter_state(last_state: SimpleState) -> void:
	pass


## Called once automatically when this state is exited.
func _exit_state(new_state: SimpleState) -> void:
	pass


## Called every frame while this state is active.
func _state_process(delta: float) -> void:
	pass


## Called every physics tick while this state is active.
func _state_physics_process(delta: float) -> void:
	pass
"

[node name="StateBot" type="Node" node_paths=PackedStringArray("starting_state")]
script = ExtResource("1_wbel7")
starting_state = NodePath("SimpleState")

[node name="SimpleState" type="Node" parent="."]
script = SubResource("GDScript_yp2gj")

[node name="SimpleState2" type="Node" parent="."]
script = SubResource("GDScript_qy167")

[node name="SimpleState3" type="Node" parent="."]
script = SubResource("GDScript_2w0hy")
